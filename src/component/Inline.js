export const Inline = () => {
  //Function Component
  const heading = {
    fontSize: "72px",
    color: "blue",
  };
  return <h1 style={heading}>Inline</h1>;
};

// In react Inline are not specify as a string
//We can use Camel Case property and the value is pecify as a strings
